name: CI/CD Pipeline

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  # ============================================================
  # STAGE 1: CREDENTIAL SCANNING
  # ============================================================
  gitleaks:
    name: Credential Scan (Gitleaks)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        id: gitleaks_scan
        uses: gitleaks/gitleaks-action@v2
        with:
          args: detect --redact --verbose --exit-code=2

  # ============================================================
  # STAGE 2: PARALLEL SCANS & TESTS (Before Build)
  # ============================================================

  frontend-checks:
    name: Frontend Tests & Linting
    runs-on: ubuntu-latest
    needs: gitleaks
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Lint with ESLint
        run: |
          if npm run | grep -q "lint"; then
            npm run lint
          else
            npx eslint src
          fi
      - name: Run Tests
        run: npm test -- --passWithNoTests

  sast-semgrep:
    name: Semgrep SAST Scan
    runs-on: ubuntu-latest
    needs: gitleaks
    permissions:
      contents: read
      security-events: write
    container:
      image: semgrep/semgrep
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run Semgrep Full Scan
        run: |
          semgrep scan --config auto --json --output semgrep-results.json .
          semgrep scan --config auto --sarif --output semgrep-results.sarif .
      - name: Upload SARIF Report
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: semgrep-results.sarif
      - name: Generate HTML Report
        if: always()
        run: |
          if [ -f .github/scripts/generate-report.py ]; then
            python3 .github/scripts/generate-report.py semgrep semgrep-results.json semgrep-report.html
          fi
      - name: Upload HTML Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: semgrep-security-report
          path: semgrep-report.html

  snyk-code:
    name: Snyk Code SAST Scan
    runs-on: ubuntu-latest
    needs: gitleaks
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run Snyk Code
        uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: code test
          args: --severity-threshold=high

  # ============================================================
  # STAGE 3: BUILD
  # ============================================================

  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: [frontend-checks, sast-semgrep, snyk-code]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build Image
        uses: docker/build-push-action@v5
        with:
          context: .
          tags: frontend-app:${{ github.sha }}
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
             GENERATE_SOURCEMAP=false
      - name: Save and Compress Image
        run: |
          # Pipe directly to zstd to avoid slow disk I/O
          docker save frontend-app:${{ github.sha }} | zstd -1 -T0 > frontend-app-image.tar.zst
      - name: Upload image
        uses: actions/upload-artifact@v4
        with:
          name: docker-image
          path: frontend-app-image.tar.zst
          retention-days: 1
          compression-level: 0

  # ============================================================
  # STAGE 4: CONTAINER SECURITY & DAST
  # ============================================================

  security-scan:
    name: Trivy Container Security Scan
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Download image
        uses: actions/download-artifact@v4
        with:
          name: docker-image
      - name: Load Image
        run: |
          zstd -d -c frontend-app-image.tar.zst | docker load
      - name: Run Trivy Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: "frontend-app:${{ github.sha }}"
          format: "json"
          output: "trivy-results.json"
          exit-code: "1"
          ignore-unfixed: true
          severity: "CRITICAL,HIGH"
      - name: Generate HTML Report
        if: always()
        run: |
          if [ -f .github/scripts/generate-report.py ]; then
            python3 .github/scripts/generate-report.py trivy trivy-results.json trivy-report.html
          fi
      - name: Upload Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-security-report
          path: |
            trivy-results.json
            trivy-report.html

  snyk-container:
    name: Snyk Container DAST Scan
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download image
        uses: actions/download-artifact@v4
        with:
          name: docker-image
      - name: Load Image
        run: |
          zstd -d -c frontend-app-image.tar.zst | docker load
      - name: Run Snyk Container Scan
        run: |
          docker run --rm \
            -e SNYK_TOKEN=${{ secrets.SNYK_TOKEN }} \
            -v /var/run/docker.sock:/var/run/docker.sock \
            snyk/snyk:docker snyk container test frontend-app:${{ github.sha }} \
            --severity-threshold=high --json > snyk-container-results.json
      - name: Upload Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: snyk-container-results
          path: snyk-container-results.json

  dast-zap:
    name: OWASP ZAP DAST Scan
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download image
        uses: actions/download-artifact@v4
        with:
          name: docker-image

      - name: Load and Run
        run: |
          zstd -d -c frontend-app-image.tar.zst | docker load
          docker run -d -p 80:80 --name zap-target \
            --add-host backend.backend-ns.svc.cluster.local:127.0.0.1 \
            frontend-app:${{ github.sha }}
          echo "ZAP_TARGET=http://172.17.0.1" >> $GITHUB_ENV
          echo "LOCAL_TARGET=http://127.0.0.1" >> $GITHUB_ENV

      - name: Wait for app to be ready
        run: |
          for i in {1..15}; do
            if curl -fsS ${{ env.LOCAL_TARGET }} >/dev/null; then
              echo "App is ready"
              exit 0
            fi
            sleep 1
          done
          echo "App did not become ready in time"
          exit 1

      - name: ZAP Scan
        uses: zaproxy/action-baseline@v0.13.0
        with:
          target: ${{ env.ZAP_TARGET }}
          cmd_options: '-a -I -m 2 -x baseline.xml -J zap-report.json'
          fail_action: true

      - name: Generate HTML Report
        if: always()
        run: |
          if [ -f .github/scripts/generate-report.py ]; then
            python3 .github/scripts/generate-report.py zap baseline.xml zap-report.html
          fi

      - name: Upload Reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: zap-reports
          path: |
            baseline.xml
            zap-report.json
            zap-report.html

  # ============================================================
  # STAGE 5: SIGN
  # ============================================================

  push-and-sign:
    name: Push to ECR & Sign with Cosign
    runs-on: ubuntu-latest
    needs: [security-scan, snyk-container, dast-zap]
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Cosign
        uses: sigstore/cosign-installer@v3.4.0

      - name: Download image
        uses: actions/download-artifact@v4
        with:
          name: docker-image

      - name: Load Image
        run: |
          zstd -d -c frontend-app-image.tar.zst | docker load

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Tag and Push Image to ECR
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
          IMAGE_TAG: frontend-${{ github.sha }}
        run: |
          docker tag frontend-app:${{ github.sha }} $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker tag frontend-app:${{ github.sha }} $ECR_REGISTRY/$ECR_REPOSITORY:frontend-latest
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:frontend-latest
          echo "IMAGE_URI=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_ENV

      - name: Sign image with Cosign
        run: |
          cosign sign --yes ${IMAGE_URI}

  # ============================================================
  # STAGE 6: SUMMARY
  # ============================================================

  summary:
    name: Pipeline Summary
    runs-on: ubuntu-latest
    needs: [gitleaks, frontend-checks, sast-semgrep, snyk-code, build, security-scan, snyk-container, dast-zap, push-and-sign]
    if: always()
    steps:
      - name: Final Summary
        run: |
          echo "### CI/CD Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "Pipeline completed for branch ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
